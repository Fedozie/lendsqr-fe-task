
import styles from "./styles/guarantors.module.scss";

const GuarantorsPage = () => {
    return (
      <section className={`${styles["guarantors-section"]}`}>
        <p className={styles.heading}>Guarantors</p>
      </section>
    );
}
 
export default GuarantorsPage;